name: Build GPS Monitor APK

on:
  push:
    paths:
      - 'apk-build/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Install Bubblewrap
        run: npm install -g @bubblewrap/cli
        timeout-minutes: 5

      - name: Initialize TWA project
        working-directory: ./apk-build
        env:
          JAVA_HOME: /opt/hostedtoolcache/Java_Temurin-Hotspot_jdk/17.0.20.1-1/x64
        run: |
          printf 'n\n%s\n\ndebug\nandroid\nn\n' "$JAVA_HOME" | bubblewrap init --manifest=manifest.json
        timeout-minutes: 10

      - name: Build APK
        working-directory: ./apk-build
        run: bubblewrap build --skipSigning
        timeout-minutes: 20

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: gps-monitor-apk
          path: apk-build/app-release.apk
          retention-days: 7
